<!-- ALDC Core Template — IMMUTABLE. Do NOT edit this file directly.
     Copy to app/requirements/{req_status}/{req_name}/{req_name}.demo-user-stories.md and fill in. -->

# Demo User Stories: {App Name}

**Date**: YYYY-MM-DD
**Author**: al-demo-architect
**Mode**: [Bootstrap / Incremental]
**Sources used**: {AL source / existing functional docs / app/requirements/{req_status}/{req_name}/*.spec.md — list what was actually available}

> These stories are derived from the app's actual pages, actions, permission sets,
> and (where available) its original spec. Every story below must trace to a real
> object — none are speculative.

---

## Actors

{One row per distinct role the app's permission sets or page audiences imply.}

| Actor | Description | Evidence (permission set / page) |
|-------|-------------|-----------------------------------|
| {Role name} | {What they do in this app} | {`<PermissionSet Name>` / `<Page Name>`} |

## Stories

{One entry per coherent capability. Group by feature area if the app is large.}

### US-01: {Short title}

**As a** {actor}
**I want to** {capability}
**So that** {value}

- **Evidence**: {object(s) this story traces to — page/action/table/codeunit, or spec section}
- **Status in app**: [Implemented / Partially implemented / Planned only]

---

## Rules

- Every story cites the object(s) or spec section it comes from — no unsupported stories.
- For **incremental** mode, only include stories new since the last manifest run;
  do not restate the full prior set here (it already lives in this same file from
  the prior run — append, don't overwrite).
- Keep story language non-technical — this document is meant to be readable by
  someone who has never seen the AL code.
