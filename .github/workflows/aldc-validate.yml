name: ALDC Core Validation

# Disabled (upstream workflow): manual dispatch only.
on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/aldc-validate
        with:
          config: aldc.yaml
